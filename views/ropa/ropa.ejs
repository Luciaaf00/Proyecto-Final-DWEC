<%- include("../template/cabecera") %>
<div class="container">
    <!--Si existen elementos en el array de Ropa-->
    <% if (arrayRopa.length > 0) { %>
     <!--Haremos el foreach para recorrer el array-->
     <% arrayRopa.forEach(ropa => { %>
       <!--Aquí podremos mostrar todos los pokemon en una tabla-->
     <% }) %>
    <% } %>
</div>
<div class="container ">
    <h1 class="d-flex flex-row justify-content-center alig-items-center">Ropa</h1>
    <a  class="btn btn-danger m-2 d-flex flex-row justify-content-center alig-items-center" href="/ropa/crear">Añadir ropa</a>
    <div class="table-responsive-sm table-responsive-md">
      <table class="table table-hover table-light">
          <thead>
              <tr class="table-dark">
                <th scope="col">Tipo de prenda</th>
                <th scope="col">Talla</th>
                <th scope="col">Color</th>
                <th scope="col">Precio</th>
                <th scope="col">Opciones</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                  <% if (arrayRopa.length>0) {%>
                      <% arrayRopa.forEach(ropa =>{ %>
                <td><%= ropa.tipo_prenda  %></td>
                <td><%= ropa.talla  %></td>
                <td><%= ropa.color  %></td>
                <td><%= ropa.precio  %></td>
                <td><a class="btn btn-warning btn-sm" href="/ropa/<%= ropa.id %>">Editar/Eliminar</a></td>
              
              </tr>
              <% }) %>
              <% } %>
            </tbody>
      </table>
  </div>

    
</div>
<%- include("../template/footer") %>